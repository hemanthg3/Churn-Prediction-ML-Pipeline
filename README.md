# Churn-Prediction-ML-Pipeline
An end-to-end machine learning pipeline that predicts customer churn using the Telco dataset. It covers data preprocessing, feature engineering, model training (Random Forest), evaluation, and saving reusable artifacts for production-ready workflows.
